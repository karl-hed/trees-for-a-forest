<div id="message-<%= message.id %>">

    <strong><%= message.sender.first_name %></strong>
    <small><i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= message.content %></p>
</div>
