<%# BANNER %>
<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.unsplash.com/photo-1597868165956-03a6827955b1?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=2070&q=80);">
  <div class="container">
  </div>
</div>

<div class="container">
  <%= cl_image_tag @user.photo.key, class:"rounded-circle w-25"%>
  <h3><%= @user.first_name %> <%= @user.last_name %></h3>
  <p>Location: <%= @user.address %></p>
  <p>Bio: <%= @user.bio %></p>
  <br>

  <%# <%= raise %>
  <div class="container">
    <p><%= @user.first_name %>'s Completed Events</p>

    <%# <% if @events_array.size > 0 %>
      <%# <% raise %>
      <div class="row">
        <div style="width: 100%; height: 600px;"
            data-controller="eventmap"
            data-eventmap-events-value="<%= @markers.to_json %>"
            data-eventmap-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"></div>
        </div>
      </div>
    <%# <% end %>



    <% if false %>
    <% @events.each do |event| %>
      <div id="events">
        <div class="card mb-5 mt-5" style="max-width: 540px;">
          <div class="row g-0">
            <div class="col-md-4">
              <%= image_tag event.organization.photo_url, class:"img-fluid ", :width=>100, :height=>100, :gravity=>"auto", :crop=>"fill" %>
            </div>
            <div class="col-md-8">
              <div class="card-body">
                <h2 class="card-title"><%= event.name %></h2>
                <h3 class="card-organization"><%= event.organization.name %></h3>
                <h5 class="card-date"><%= event.date %></h5>
                <div class="btn btn-flat">
                  <%= link_to "View", event_path(event), class:"text-decoration-none fw-semibold " %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% end %>
    <% end %>

  </div>




  <div class="card w-25px">
    <div class="card-body">
      carpool <i class="fa-solid fa-car-side"></i>
    </div>
  </div>

  <button type="button" class="btn btn-flat mt-5"><i class="fa-solid fa-messages"></i><i class="fa-solid fa-comment"></i>Send a message </button>

  <p class="my-5">
    <% if @user.wants_to_carpool %>
      <%= @user.first_name + " accepts carpooling requests" %>
    <% else %>
      <%= @user.first_name + " doesn't accepts carpooling requests" %>
    <% end %>
  </p>


  <%# <button type="button" class="btn btn-success btn-lg"><i class="fa-solid fa-messages"></i>Messege</button> %>
  <%= link_to "messege user", profile_messages_path(@user.id), class:"mb-5" %>

</div>
